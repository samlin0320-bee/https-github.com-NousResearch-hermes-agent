# NOTE: This file is maintained for convenience only.
# The canonical dependency list is in pyproject.toml.
# Preferred install: pip install -e ".[all]"

# Core dependencies
openai
python-dotenv
fire
httpx
rich
tenacity
prompt_toolkit
pyyaml
requests
jinja2
pydantic>=2.0
PyJWT[crypto]
debugpy

# Web tools
firecrawl-py
parallel-web>=0.4.2

# Image generation
fal-client

# Text-to-speech (Edge TTS is free, no API key needed)
edge-tts

# Offline data sovereignty: language-agnostic summarization and compression
# without LLM dependency. Required for compression.flush_strategy=offline
# and compression.summary_strategy=offline. Both are encoder-only models
# (GLiNER2 205M + XLM-RoBERTa 355M), multilingual, CPU/GPU compatible.
llmlingua
gliner2

# Optional: For cron expression parsing (cronjob scheduling)
croniter

# Optional: For messaging platform integrations (gateway)
python-telegram-bot[webhooks]>=22.6
discord.py>=2.0
aiohttp>=3.9.0
